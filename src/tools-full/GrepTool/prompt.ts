/**
 * Grep Tool Prompt - 常量
 */

export const GREP_TOOL_NAME = 'grep';

export function getDescription(): string {
  return 'Search for text patterns in files using regular expressions (powered by ripgrep)';
}
