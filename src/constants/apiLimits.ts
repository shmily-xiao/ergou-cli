/**
 * API Limits - 常量
 */

export const DEFAULT_FILE_READ_LIMIT = 1024 * 1024; // 1MB
export const DEFAULT_FILE_WRITE_LIMIT = 10 * 1024 * 1024; // 10MB
export const DEFAULT_GREP_HEAD_LIMIT = 100;
export const DEFAULT_GLOB_HEAD_LIMIT = 100;
